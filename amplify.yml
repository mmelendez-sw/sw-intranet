version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  customHeaders:
    - pattern: '**/*'
      headers:
        - key: 'Cache-Control'
          value: 'public, max-age=0, must-revalidate'
  customRules:
    - source: '/api/<*>'
      target: 'https://coc6leb5vo5ljujvoqcjozkocy0ihyan.lambda-url.us-east-2.on.aws/api/<*>'
      status: '200'
    - source: '/<*>'
      target: '/index.html'
      status: '200'